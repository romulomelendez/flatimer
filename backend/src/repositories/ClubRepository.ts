import { Club } from "../entities/Club"
import { IClub } from "../interfaces/IClub"

export class ClubRepository implements IClub {

    public constructor() {}

    // findById(id: string): Club {
        
    // }

    findByName(name: string): Club {
        
    }
}