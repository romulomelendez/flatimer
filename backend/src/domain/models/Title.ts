export interface Title {
    name: string,
    lastTitle: {
        name: string,
        date: number,
        years: number,
        mounths: number,
        days: number,
        hours: number,
        minutes: number,
        seconds: number
    }
}